# 应用名称
spring:
  application:
    name: provider-service-2

# 服务器端口
server:
  port: 8082

# Dubbo配置
dubbo:
  application:
    name: provider-service-2
  registry:
    address: nacos://localhost:8848
    username: nacos
    password: nacos
  protocol:
    name: dubbo
    port: 20882  # 不同的端口
  scan:
    base-packages: com.example.dubbo.provider.service
